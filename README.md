# Telegram Bot with DeepSeek AI Integration

Асинхронный Telegram-бот на aiogram 3.x с интеграцией DeepSeek API, админ-панелью, системой флаеров и реферальной системой.

## Функциональность

- 🤖 Интеграция с DeepSeek AI API
- 👥 Реферальная система
- 📄 Система флаеров (обычные и настраиваемые в админке)
- 💰 Пополнение баланса через Telegram Stars
- 📢 Система массовых рассылок
- 👨‍💼 Расширенная админ-панель со статистикой и управлением пользователями
- 🛡️ Защита от спама и rate limiting
- 📊 Подробная статистика и аналитика

## Требования

- Python 3.9+
- SQLite (для разработки) или PostgreSQL (для продакшена)
- Redis (опционально)

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd deepseek-ai-bot
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
```

Отредактируйте `.env` файл, добавив необходимые значения:
- `BOT_TOKEN` - токен вашего Telegram бота
- `ADMIN_IDS` - ID администраторов через запятую
- `DEEPSEEK_API_KEY` - ключ API DeepSeek

## Запуск

### Режим разработки
```bash
python main.py
```

### С Redis (для продакшена)
Убедитесь, что Redis запущен, затем:
```bash
python main.py
```

## Структура проекта

```
project/
├── app/
│   ├── handlers/          # Обработчики команд
│   │   ├── admin.py       # Админ-панель
│   │   ├── user.py        # Пользовательские команды
│   │   ├── deepseek.py    # DeepSeek интеграция
│   │   └── referral.py    # Реферальная система
│   ├── keyboards/         # Клавиатуры
│   │   ├── admin.py       # Админ клавиатуры
│   │   └── user.py        # Пользовательские клавиатуры
│   ├── middlewares/       # Промежуточное ПО
│   │   ├── throttling.py  # Rate limiting
│   │   └── acl.py         # Контроль доступа
│   ├── services/          # Бизнес-логика
│   │   ├── deepseek_api.py # API DeepSeek
│   │   ├── mailing.py     # Система рассылок
│   │   └── referral.py    # Реферальная система
│   ├── models/            # Модели базы данных
│   │   ├── user.py        # Пользователи
│   │   ├── referral.py    # Рефералы
│   │   ├── flyer.py       # Флаеры
│   │   └── mailing.py     # Рассылки
│   ├── utils/             # Утилиты
│   │   ├── states.py      # FSM состояния
│   │   ├── helpers.py     # Вспомогательные функции
│   │   └── filters/       # Кастомные фильтры
├── migrations/            # Миграции БД
├── config.py              # Конфигурация
├── database.py            # Настройки БД
├── main.py                # Точка входа
├── requirements.txt       # Зависимости
└── README.md             # Документация
```

## Команды бота

### Пользовательские команды
- `/start` - начать работу с ботом
- `/help` - справка
- `/deepseek` - доступ к DeepSeek AI
- `/flyers` - просмотр флаеров
- `/referral` - реферальная система
- `/profile` - профиль пользователя

### Админ команды
- `/admin` - админ-панель
- `/stats` - статистика
- `/mailing` - рассылка
- `/users` - управление пользователями
- `/add_flyer` - добавить флаер
- `/set_deepseek` - настройка DeepSeek API

## API интеграции

### DeepSeek API
Бот интегрирован с DeepSeek API для обработки запросов ИИ.

Поддерживаемые модели:
- `deepseek-chat`
- `deepseek-coder`

### База данных
Поддержка SQLite (разработка) и PostgreSQL (продакшен).

## Безопасность

- Rate limiting (5 сообщений/секунду)
- Проверка прав администратора
- Валидация входящих данных
- Шифрование чувствительных данных

## Развертывание

### Docker (рекомендуется)
```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
CMD ["python", "main.py"]
```

### Системные требования
- Минимум 1 ГБ RAM
- 2 vCPU
- 20 ГБ дискового пространства

## Логирование

Логи сохраняются в файлы с ротацией. Уровни:
- INFO: основные действия
- WARNING: подозрительная активность
- ERROR: ошибки
- DEBUG: отладка

## Разработка

### Добавление новых команд
1. Создайте handler в `app/handlers/`
2. Зарегистрируйте в `main.py`
3. Добавьте клавиатуру в `app/keyboards/`

### Добавление моделей БД
1. Создайте модель в `app/models/`
2. Добавьте в `__init__.py`
3. Создайте миграцию: `alembic revision --autogenerate`

## Лицензия

MIT License